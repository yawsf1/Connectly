{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "nixpacks",
        "nixpacksConfigPath": "nixpacks.toml"
    },
    "deploy": {
        "startCommand": "php artisan migrate --force && php artisan config:clear && php artisan cache:clear && php artisan storage:link --force && php artisan serve --host=0.0.0.0 --port=${PORT}"
    }
}
