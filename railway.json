{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "nixpacks"
    },
    "deploy": {
        "startCommand": "echo $APP_KEY && echo $DB_HOST && php artisan migrate --force && php artisan config:clear && php artisan serve --host=0.0.0.0 --port=8068"
    }
}
